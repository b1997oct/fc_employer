import { useEffect } from 'react'

export default function useBody(open) {
    // useEffect(() => {
    //     if (open) {
    //         let width = window.innerWidth > 768
    //         width = `overflow: hidden; ${width ? 'padding-right:16px;' : ''}`
    //         document.body.style = width
    //     } else {
    //         document.body.style = ''
    //     }
    // }, [open])
}
